<% if notification.read_at? %>
  <%= link_to patient_path(notification.notifiable.id), class: "dropdown-item read" do %>
    <%= notification.actor.name %>
    <%= notification.action %>
    a <%= notification.notifiable_type.underscore.humanize.downcase %>
  <% end %>
<% else %>
  <%= link_to patient_path(notification.notifiable.id), class: "dropdown-item unread" do %>
    <%= notification.actor.name %>
    <%= notification.action %>
    a <%= notification.notifiable_type.underscore.humanize.downcase %>
  <% end %>
<% end %>
